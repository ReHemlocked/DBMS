import React from "react";
import { useState } from "react";

// this file must handle user profiles
// Take inputs Name, Bio, Profile_Pic, Gender and public_or_private
// 3 fields username, email and password(encrypted) must also be displayed as read only.